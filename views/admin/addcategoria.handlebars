{{#each erros}}
<div class="alert alert-danger"> {{texto}}</div>
{{else}}
{{/each}}

<div class="card">
    <h5 class="card-header">
        New category
    </h5>
    <div class="card-body">
        <form action="/admin/categoria/new" method="post">
            <label for="nome">Nome: </label>
            <input type="text" name="nome" id="nome" placeholder="your name" class="form-control">
            <br>
            <label for="slug">Slug: </label> {{!-- expecie de url --}}
            <input type="text" name="slug" id="slug" placeholder="Slug the category" class="form-control">
            <br>
            <button class="btn btn-dark" type="submit">Add categoria</button>
        </form>

    </div>

</div>